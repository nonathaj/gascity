name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y tmux jq

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install mcp_agent_mail
        run: pip install mcp-agent-mail

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install tools
        run: make install-tools

      - name: Lint
        run: make lint

      - name: Format
        run: make fmt-check

      - name: Vet
        run: make vet

      - name: Test
        run: make test-cover

      - name: Integration tests
        run: make test-integration

      - name: MCP mail conformance test
        continue-on-error: true  # mcp_agent_mail API may drift; don't block CI
        run: make test-mcp-mail

      - name: Docker session tests
        run: make test-docker

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: coverage.txt
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true # optional (default = false)
